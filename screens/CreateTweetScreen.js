import { StyleSheet, Text, View } from "react-native";

const CreateTweetScreen = () => {
	return (
		<View>
			<Text>This is CreateTweetScreen !!</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default CreateTweetScreen;
