import { StyleSheet, Text, View } from "react-native";

const TweetScreen = () => {
	return (
		<View>
			<Text>This is TweetScreen !!</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default TweetScreen;
