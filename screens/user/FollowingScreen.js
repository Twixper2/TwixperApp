import { StyleSheet, Text, View } from "react-native";

const FollowingScreen = () => {
	return (
		<View>
			<Text>This is FollowingScreen !!</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default FollowingScreen;
